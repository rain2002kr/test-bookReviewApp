<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"

    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <ImageView
        android:id="@+id/bookImageView"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        android:background="@drawable/background_round_16dp"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:padding="8dp"

        />
    <TextView
        android:id="@+id/bookTitleTextView"
        app:layout_constraintStart_toEndOf="@id/bookImageView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="24sp"
        android:background="@drawable/background_stroke"
        tools:text="매매의 기술"
        android:layout_marginStart="16dp"
        android:maxLines="1"
        android:ellipsize="end"
        />

    <TextView
        android:id="@+id/bookDescriptionTextView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="10dp"
        android:ellipsize="end"
        android:maxLines="4"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/bookImageView"
        app:layout_constraintTop_toBottomOf="@id/bookTitleTextView"
        tools:text="매매의 기술은 어느 누구보다 내가 잘알고 있다고 자부한다.매매의 기술은 어느 누구보다 내가 잘알고 있다고 자부한다.매매의 기술은 어느 누구보다 내가 잘알고 있다고 자부한다." />



</androidx.constraintlayout.widget.ConstraintLayout>